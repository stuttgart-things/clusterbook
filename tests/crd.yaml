---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: networkconfigs.clusterbook.stuttgart-things.com
spec:
  group: clusterbook.stuttgart-things.com
  names:
    kind: NetworkConfig
    listKind: NetworkConfigList
    plural: networkconfigs
    singular: networkconfig
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                networks:  # Corrected property name from 'newtorks' to 'networks'
                  type: object
                  additionalProperties:
                    type: array
                    items:
                      type: string
---
apiVersion: clusterbook.stuttgart-things.com/v1
kind: NetworkConfig
metadata:
  name: networks-labul
  namespace: default  # Ensure this is the correct namespace
spec:
  networks:  # Corrected property name from 'newtorks' to 'networks'
    "10.31.103":
      - "3:assigned:sandiego"
      - "4"
    "10.31.104":
      - "8:pending:miami"
      - "10"
      - "11"