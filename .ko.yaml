---
defaultBaseImage: ghcr.io/stuttgart-things/sthings-alpine:1.23
builds:
  - id: clusterbook
    dir: .
    main: main.go
    ldflags:
      - -s -w -X github.com/stuttgart-things/clusterbook/internal.version={{.Env.VERSION}}
      - -s -w -X github.com/stuttgart-things/clusterbook/internal.date={{.Env.BUILD_DATE}}
      - -s -w -X github.com/stuttgart-things/clusterbook/internal.commit={{.Env.COMMIT}}
